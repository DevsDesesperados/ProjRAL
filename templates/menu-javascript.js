$(document).ready(function() {
  var botao = $('.bt1'); //classe no "a" que vai ser clicado.
  var dropDown = $('.ul-ral'); //classe do submenu que vai ser aberto "ul".

     botao.on('click', function(event){
         dropDown.stop(true,true).slideToggle();
         event.stopPropagation();
     });
 });
 //Subtituir as 2 classes

 $(document).ready(function() {
  var botao = $('.bt2'); //classe no "a" que vai ser clicado.
  var dropDown = $('.ul-lavra'); //classe do submenu que vai ser aberto "ul".

     botao.on('click', function(event){
         dropDown.stop(true,true).slideToggle();
         event.stopPropagation();
     });
 });
 //Subtituir as 2 classes

 $(document).ready(function() {
  var botao = $('.bt3'); //classe no "a" que vai ser clicado.
  var dropDown = $('.ul-beneficiamento'); //classe do submenu que vai ser aberto "ul".

     botao.on('click', function(event){
         dropDown.stop(true,true).slideToggle();
         event.stopPropagation();
     });
 });
 //Subtituir as 2 classes

 $(document).ready(function() {
  var botao = $('.bt4'); //classe no "a" que vai ser clicado.
  var dropDown = $('.ul-cadastro'); //classe do submenu que vai ser aberto "ul".

     botao.on('click', function(event){
         dropDown.stop(true,true).slideToggle();
         event.stopPropagation();
     });
 });
 //Subtituir as 2 classes